<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title><%= book.title %> - MoodVerse</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <main>
    <div class="book-detail-container">
      <h1 class="book-detail-title"><%= book.title %></h1>
      <p class="book-detail-author">by <%= book.author %></p>

      <% if (book.imageUrl) { %>
        <img class="book-cover" src="<%= book.imageUrl %>" alt="<%= book.title %> cover">
      <% } %>

      <p class="book-detail-description"><%= book.desc %></p>

      <% if (book.moodtags && book.moodtags.length > 0) { %>
        <div class="book-detail-tags">
          <% book.moodtags.forEach(tag => { %>
            <span class="tag"><%= tag %></span>
          <% }) %>
        </div>
      <% } %>

      <p><em>Uploaded by: <%= book.uploadedBy %></em></p>

      <a href="/books" class="book-detail-back">← Back to All Books</a>
    </div>
  </main>
</body>
</html>
